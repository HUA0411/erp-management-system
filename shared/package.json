{
  "name": "@erp/shared",
  "version": "1.0.0",
  "private": true,
  "description": "前后端共享类型定义（仅类型，无运行时逻辑，import type 使用）",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
